<li class="list-group-item list-group-item-info">
  <% if a.trackable %>
    <%= render "shared/activity_owner", a: a %> liked
    <%= a.recipient.name %>'s review in
    <%= link_to a.trackable.location.name, location_path(a.trackable) %>
    <%= time_ago_in_words activity.created_at %> <%= t :ago %>
  <% else %>
    A review had been deleted
  <% end %>
</li>
